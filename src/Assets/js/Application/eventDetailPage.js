let event;

export function initializeEventDetailPage(event) {
    this.event = event;
    display();
}

export function display() {
    // display event.title
    // display event.description
    // display event.start
    // display event.end
}